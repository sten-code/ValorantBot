# Arduino Leonardo

You have 2 choices here. If you have the USB Host Shield, you can use the `mouse_usb_host_shield` firmware. If you don't have the USB Host Shield, you can use the `mouse` firmware. However, the `mouse` firmware is a bit more complicated to set up.
